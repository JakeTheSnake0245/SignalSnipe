# Changelog

## v1.3
- Stable CoT UID based on sensor_name/callsign
- GeoChat send on DETECT (b-t-f)
- MGRS (manual_mgrs) conversion support when `python3-mgrs` is available
- Hardened rtl_power handling (timeout + parse last CSV line)
- Systemd service config + kill behavior improvements
